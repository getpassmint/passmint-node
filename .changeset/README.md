# Changesets

This folder contains [changesets](https://github.com/changesets/changesets) — short markdown files that describe pending changes to the package. Each one bumps the version and lands a CHANGELOG entry when released.

Run `pnpm changeset` to create one interactively.
